# 唯一必填项：由 底研 签发的租户凭据。使用者在 MCP 详情页填写。
DIYAN_API_KEY=

# 可选：仅在自建部署或联调时覆盖。必须是 HTTPS。
# DIYAN_BACKEND_URL=https://api.diyan.example/v1

# 可选：单次请求超时秒数，允许 1-120，超出范围回落到 30。
# DIYAN_TIMEOUT_SECONDS=30

# 可选：stdio / sse / streamable-http，默认 sse。
# DIYAN_TRANSPORT=sse
